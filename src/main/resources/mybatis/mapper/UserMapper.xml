<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zky.dao.UserDao">

    <resultMap id="BaseResultMap" type="com.zky.domain.po.UserInfo">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="age" property="age" />
        <result column="gender" property="gender" />
        <result column="city" property="city" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <insert id="insert" parameterType="com.zky.domain.po.UserInfo">
        INSERT INTO user_info (user_id, username, password, phone, age, gender, city, create_time, update_time)
        VALUES (#{userId}, #{username}, #{password}, #{phone}, #{age}, #{gender}, #{city}, NOW(), NOW())
    </insert>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM user_info WHERE user_id = #{userId}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM user_info WHERE username = #{username}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM user_info
    </select>

    <update id="update" parameterType="com.zky.domain.po.UserInfo">
        UPDATE user_info
        SET username = #{username},
            password = #{password},
            phone = #{phone},
            age = #{age},
            gender = #{gender},
            city = #{city},
            update_time = NOW()
        WHERE user_id = #{userId}
    </update>

</mapper>
