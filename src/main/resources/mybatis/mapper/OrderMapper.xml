<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zky.dao.OrderDao">

    <resultMap id="OrderResultMap" type="com.zky.domain.po.OrderInfo">
        <id column="id" property="id" />
        <result column="order_id" property="orderId" />
        <result column="user_id" property="userId" />
        <result column="total_amount" property="totalPrice" />
        <result column="status" property="status" />
        <result column="address" property="address" />
        <result column="contact_name" property="contactName" />
        <result column="contact_phone" property="contactPhone" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    
    <resultMap id="OrderItemResultMap" type="com.zky.domain.po.OrderItem">
        <id column="id" property="id" />
        <result column="order_id" property="orderId" />
        <result column="product_id" property="productId" />
        <result column="product_name" property="productName" />
        <result column="product_image" property="productImage" />
        <result column="current_price" property="price" />
        <result column="quantity" property="quantity" />
    </resultMap>

    <insert id="insertOrder" parameterType="com.zky.domain.po.OrderInfo">
        INSERT INTO order_info (order_id, user_id, total_amount, status, address, contact_name, contact_phone, create_time, update_time)
        VALUES (#{orderId}, #{userId}, #{totalPrice}, #{status}, #{address}, #{contactName}, #{contactPhone}, NOW(), NOW())
    </insert>

    <insert id="insertOrderItem" parameterType="com.zky.domain.po.OrderItem">
        INSERT INTO order_item (item_id, order_id, product_id, product_name, product_image, current_price, quantity, total_price, create_time, update_time)
        VALUES (UUID(), #{orderId}, #{productId}, #{productName}, #{productImage}, #{price}, #{quantity}, #{price} * #{quantity}, NOW(), NOW())
    </insert>

    <select id="selectOrderById" resultMap="OrderResultMap">
        SELECT * FROM order_info WHERE order_id = #{orderId}
    </select>

    <select id="selectOrderItemsByOrderId" resultMap="OrderItemResultMap">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

    <select id="selectOrdersByUserId" resultMap="OrderResultMap">
        SELECT * FROM order_info WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

</mapper>
