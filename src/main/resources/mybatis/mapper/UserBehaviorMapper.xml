<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zky.dao.UserBehaviorDao">

    <resultMap id="BaseResultMap" type="com.zky.domain.po.UserBehavior">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="behavior_type" property="behaviorType"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, user_id, product_id, behavior_type, create_time
        FROM user_behavior
    </select>

    <insert id="insert" parameterType="com.zky.domain.po.UserBehavior">
        INSERT INTO user_behavior (user_id,
                                   product_id,
                                   behavior_type,
                                   create_time)
        VALUES (#{userId},
                #{productId},
                #{behaviorType},
                #{createTime})
    </insert>

    <select id="selectProductIdsWithBehavior" resultType="java.lang.String">
        SELECT DISTINCT product_id
        FROM user_behavior
        WHERE create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
    </select>

    <select id="selectProductIdsByUserAndDays" resultType="java.lang.String">
        SELECT DISTINCT product_id
        FROM user_behavior
        WHERE user_id = #{userId}
          AND create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
    </select>



    <select id="selectDistinctUserCountByProductId" resultType="java.lang.Integer">
        SELECT COUNT(DISTINCT user_id)
        FROM user_behavior
        WHERE product_id = #{productId}
          AND create_time >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
          AND behavior_type IN ('CLICK','COLLECT','BUY')
    </select>


    <!-- 查询指定用户品类行为加权统计：view/click=1, collect/cart=3, buy=5 -->
    <select id="selectCategoryWeightByUser" resultType="java.util.Map">
        SELECT p.category,
               SUM(CASE ub.behavior_type
                   WHEN 'VIEW'    THEN 1
                   WHEN 'CLICK'   THEN 1
                   WHEN 'COLLECT' THEN 3
                   WHEN 'CART'    THEN 3
                   WHEN 'BUY'     THEN 5
                   ELSE 1 END) AS totalWeight
        FROM user_behavior ub
        INNER JOIN product_info p ON ub.product_id = p.product_id
        WHERE ub.user_id = #{userId}
          AND p.category IS NOT NULL
        GROUP BY p.category
        ORDER BY totalWeight DESC
    </select>

</mapper>
