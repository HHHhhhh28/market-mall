<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zky.dao.UserBehaviorDao">

    <resultMap id="BaseResultMap" type="com.zky.domain.po.UserBehavior">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="behavior_type" property="behaviorType"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, user_id, product_id, behavior_type, create_time
        FROM user_behavior
    </select>

    <insert id="insert" parameterType="com.zky.domain.po.UserBehavior">
        INSERT INTO user_behavior (user_id,
                                   product_id,
                                   behavior_type,
                                   create_time)
        VALUES (#{userId},
                #{productId},
                #{behaviorType},
                #{createTime})
    </insert>

</mapper>
